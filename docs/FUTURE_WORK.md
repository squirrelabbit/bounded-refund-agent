# FUTURE WORK

One line per idea. Not implemented, not planned into the current scope.

- Partial-refund ledger so a payment can be refunded more than once up to the captured amount.
- Permit scopes covering multiple resources for a single multi-step plan.
- A replay tool that reconstructs final state from the audit JSONL alone.
- Adversarial proposal generation instead of hand-written malicious proposals.
- A second provider adapter to compare live planners under the same policy boundary.
- Property-based tests over the order state machine.
